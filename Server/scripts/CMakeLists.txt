file( GLOB_RECURSE SCRIPT_FILES LIST_DIRECTORIES false CONFIGURE_DEPENDS "*.fos" )
list( APPEND CONTENT_FILES ${SCRIPT_FILES} )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/scripts.cfg )
set( CONTENT_FILES "${CONTENT_FILES}" PARENT_SCOPE )
