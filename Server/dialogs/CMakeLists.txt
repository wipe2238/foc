file( GLOB_RECURSE DIALOGS_FILES LIST_DIRECTORIES false CONFIGURE_DEPENDS "*.fodlg" )
list( APPEND CONTENT_FILES ${DIALOGS_FILES} )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/dialogs.cfg )
set( CONTENT_FILES "${CONTENT_FILES}" PARENT_SCOPE )
