file( GLOB_RECURSE MAPS_FILES LIST_DIRECTORIES false CONFIGURE_DEPENDS "*.fomap" )
list( APPEND CONTENT_FILES ${MAPS_FILES} )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/Locations.cfg )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/GenerateWorld.cfg )
set( CONTENT_FILES "${CONTENT_FILES}" PARENT_SCOPE )
