file( GLOB_RECURSE PROTO_FILES LIST_DIRECTORIES false CONFIGURE_DEPENDS "*.fopro" )
list( APPEND CONTENT_FILES ${PROTO_FILES} )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/critters/critters.lst )
list( APPEND CONTENT_FILES ${CMAKE_CURRENT_LIST_DIR}/items/items.lst )
set( CONTENT_FILES "${CONTENT_FILES}" PARENT_SCOPE )
